destructuringAssignabilityCheck.ts(1,7): error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
destructuringAssignabilityCheck.ts(3,3): error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
destructuringAssignabilityCheck.ts(4,3): error TS100090: A destructured parameter cannot traverse a nullable source. Bind the parameter first and destructure it in the function body.
destructuringAssignabilityCheck.ts(6,14): error TS100090: A destructured parameter cannot traverse a nullable source. Bind the parameter first and destructure it in the function body.
destructuringAssignabilityCheck.ts(9,14): error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
destructuringAssignabilityCheck.ts(15,7): error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.


==== destructuringAssignabilityCheck.ts (6 errors) ====
    const [] = {}; // should be error
          ~~
!!! error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
    const {} = undefined; // error correctly
    (([]) => 0)({}); // should be error
      ~~
!!! error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
    (({}) => 0)(undefined); // should be error
      ~~
!!! error TS100090: A destructured parameter cannot traverse a nullable source. Bind the parameter first and destructure it in the function body.
    
    function foo({}: undefined) {
                 ~~
!!! error TS100090: A destructured parameter cannot traverse a nullable source. Bind the parameter first and destructure it in the function body.
        return 0
    }
    function bar([]: {}) {
                 ~~~~~~
!!! error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
        return 0
    }
    
    const { }: undefined = 1
    
    const []: {} = {}
          ~~
!!! error TS2488: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.
    