//// [tests/cases/conformance/kvs/kvsDefaultConstructorImports.ts] ////

=== model.ts ===
export class Session {
>Session : Symbol(Session, Decl(model.ts, 0, 0))

    constructor(public name = "") {}
>name : Symbol(Session.name, Decl(model.ts, 1, 16))
}

=== main.ts ===
import { Session as LocalSession } from "./model";
>Session : Symbol(LocalSession, Decl(model.ts, 0, 0))
>LocalSession : Symbol(LocalSession, Decl(main.ts, 0, 8))

declare const maybeSession: LocalSession?;
>maybeSession : Symbol(maybeSession, Decl(main.ts, 2, 13))
>LocalSession : Symbol(LocalSession, Decl(main.ts, 0, 8))

export const session = maybeSession!;
>session : Symbol(session, Decl(main.ts, 3, 12))
>maybeSession : Symbol(maybeSession, Decl(main.ts, 2, 13))

